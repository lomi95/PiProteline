% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter_by_pvalue_fdr.R
\name{filter_by_pvalue_fdr}
\alias{filter_by_pvalue_fdr}
\title{Filter a Data Frame by P-value or Adjusted P-value (FDR)}
\usage{
filter_by_pvalue_fdr(df, filter_pvalue_or_fdr, significance_threshold)
}
\arguments{
\item{df}{A data frame containing the p-values or adjusted p-values.}

\item{filter_pvalue_or_fdr}{A character string indicating the column to filter by. Valid
values are "p.value" or "p.adj" (case-insensitive). Default is "p.value".}

\item{significance_threshold}{A numeric value specifying the significance threshold. Rows with
values less than this threshold are retained. Default is 0.05.}
}
\value{
A data frame filtered to include only rows where the specified column's
  values are below the threshold.
}
\description{
Filter a Data Frame by P-value or Adjusted P-value (FDR)
}
\examples{
# Example data frame
df <- data.frame(
  gene = c("Gene1", "Gene2", "Gene3"),
  p.value = c(0.01, 0.05, 0.1),
  p.adj = c(0.05, 0.1, 0.2)
)

# Filter by p.value < 0.01
filter_by_pvalue_fdr(df, "p.value", 0.051)

# Filter by p.adj < 0.05
filter_by_pvalue_fdr(df, "p.adj", 0.051)

}
